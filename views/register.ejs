<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title><%= title %></title>
  <%- include("./_share/stylesheets.ejs") %>
</head>
<body class="text-center">
  <form class="form-signin" method="POST" action="/register/input">
    <div class="mx-auto" style="width: 400px;">
      <div class="border-box">
        <h1 class="h3 mb-5 mt-5 font-weight-normal">Sign Up</h1>
        <label for="inputName" class="sr-only">Name</label>
        <input name="username" type="username" id="inputName" class="form-control" placeholder="Name"  autofocus>
        <label for="inputEmail" class="sr-only">Email address</label>
        <input name="email" type="email" id="inputEmail" class="form-control" placeholder="Email address"  autofocus>
        <label for="inputPassword" class="sr-only">Password</label>
        <input name="password" type="password" id="inputPassword" class="form-control" placeholder="Password" >
        <label for="inputPassword" class="sr-only">Password</label>
        <input name="repassword" type="password" id="inputPassword" class="form-control" placeholder="Conform" >
        <div class="box  mb-5 mt-5"> </div>
        <% if (locals.errors) { %>
          <% if (errors && errors.length !== 0) { for(let i = 0 ; i < errors.length;i++) { %>
          <div class="alert alert-danger" ><%= errors[i].msg %></div>
        <% }}} %>
        <button class="btn btn-lg btn-primary btn-block" type="submit">OK</button>
        <a class="link" href="/" class="stretched-link">sign in</a>
      </div>
    </div>
  </form>
</body>
</html>